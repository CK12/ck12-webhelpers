--- session.py.orig	2014-05-23 09:37:16.000000000 -0700
+++ session.py	2014-05-23 09:39:14.000000000 -0700
@@ -7,10 +7,12 @@
 from beaker.cache import clsmap
 from beaker.exceptions import BeakerException, InvalidCryptoBackendError
 from base64 import b64encode, b64decode
+import logging
 
 
 __all__ = ['SignedCookie', 'Session']
 
+log = logging.getLogger(__name__)
 
 try:
     import uuid
@@ -307,6 +309,9 @@
         if self.use_cookies:
             self._delete_cookie()
         self.clear()
+        if hasattr(self, 'namespace') and self.namespace and 'session' in self.namespace:
+            log.debug("Deleting session for namespace[%s] for class[%s]" % (self.namespace.namespace, self.namespace.__class__.__name__))
+            del self.namespace['session']
 
     def invalidate(self):
         """Invalidates this session, creates a new session id, returns
